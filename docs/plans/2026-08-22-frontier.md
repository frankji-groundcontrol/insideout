# 2026-08-22 — Frontier: audience web view, proposal application, canvas cursors

Status: **complete** (2026-08-22). The honest frontier recorded in
HANDOFF after the depth thread.

## Checklist

- [x] Audience-view web rendering: `/prd/{id}/view` over the existing
      view API — ordered sections with whys, gaps, latest Commit,
      disclosure; routes tested, API live-verified
- [x] Proposal→structure application: `proposal_items` at propose,
      `apply` on acceptance creates nodes as the deciding human —
      live-verified (17→18 nodes, proposed title present); RLS bug in
      the apply path found live and fixed
- [x] Canvas cursors: registry broadcast + cursor route + SSE `cursor`
      events + web overlay (labeled pointers, throttled send, leave
      cleanup) — live-verified (exact coordinates delivered)
- [x] Docs closure; checkpoint pushed
      ([changelog](../changelogs/2026-08-22-frontier.md))
