# 2026-08-22 — Depth thread: revision verbs, presence, time-first roadmap

Status: **complete** (2026-08-22). The three follow-on threads from the
week's arc.

## Checklist

- [x] PRD-revision verbs: CLI `revisions` / `snapshot`, MCP tools —
      live-verified through the domain (parity Stage 3 closed)
- [x] Real-time presence: registry + SSE stream + snapshot + web chips
      + CLI/MCP `presence` verbs — live-verified with two tabs joining
      and leaving; non-member 403 enforced
- [x] Time-first roadmap: `deadline` column, pressure states, Progress
      view (Now deadlined-only, ≤3 Next, Done, needsDeadline), web
      chips + picker + progress strip, CLI/MCP verbs — live-verified
      set/clear flows
- [x] Docs closure; checkpoint pushed
      ([changelog](../changelogs/2026-08-22-depth-thread.md))

## Sources

- PRODUCT.md "Time is a first-class constraint", "The shared Roadmap
  Graph" (Progress view), "Collaboration and authority" (presence)
