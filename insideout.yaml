# InsideOut matching guide — maps GitHub activity in this repo onto
# roadmap nodes of "InsideOut 自举".
#
# An event (push, pull request, deployment) attaches evidence to every
# node it matches that is a LEAF. Matchers:
#   branches: exact branch name or prefix ending in /*  (event's branch)
#   labels:   exact pull-request label (e.g. roadmap/mvp)
#   paths:    prefix of any file the event touched   (e.g. server/)
# Unmatched events stay visible in InsideOut but attach to nothing.
# Re-generate any time with `insideout guide <project-id>`.
version: 1
nodes:
  # 146724e9-859c-44ce-abe0-dacdcfcfbb0a is a branch node — matchers apply to leaves only:
  # 146724e9-859c-44ce-abe0-dacdcfcfbb0a:
  #   title: "完成自举 MVP"
  d1f9c245-3819-4afd-ab42-5e4a6d78bd06:
    title: "确认交付约束"
    branches: [] # e.g. [feature/x, main]
    labels:   [] # e.g. [roadmap/mvp]
    paths:    [] # e.g. ["server/"]
  2f9ff654-5f93-44a8-bbc5-7648d4b78b2c:
    title: "管理交付风险"
    branches: [] # e.g. [feature/x, main]
    labels:   [] # e.g. [roadmap/mvp]
    paths:    [] # e.g. ["server/"]
  d715fbca-1558-4161-b087-accdc6939ed1:
    title: "编写用户故事"
    branches: [] # e.g. [feature/x, main]
    labels:   [] # e.g. [roadmap/mvp]
    paths:    [] # e.g. ["server/"]
  17162045-36a1-41e4-949d-2f05c9ab464a:
    title: "拆解核心需求"
    branches: [main]
    labels:   []
    paths:    []
  # 75e00a33-87a5-49c8-b246-7fbf2f37c7b6 is a branch node — matchers apply to leaves only:
  # 75e00a33-87a5-49c8-b246-7fbf2f37c7b6:
  #   title: "产品定义"
  # 1004f1aa-d49b-446c-a2ff-be9a68c068b7 is a branch node — matchers apply to leaves only:
  # 1004f1aa-d49b-446c-a2ff-be9a68c068b7:
  #   title: "验证最小闭环"
  # 059a7fc7-a561-478d-a26a-c8432407cda9 is a branch node — matchers apply to leaves only:
  # 059a7fc7-a561-478d-a26a-c8432407cda9:
  #   title: "准备交付基础"
  9906a55b-e5e0-4ae4-b0a4-8f6c0b27e10a:
    title: "明确用户问题"
    branches: []
    labels:   []
    paths:    ["server/"]
  da2c7d12-1f11-493f-bb28-6c67e6163d16:
    title: "划定 MVP 边界"
    branches: []
    labels:   []
    paths:    ["client/"]
